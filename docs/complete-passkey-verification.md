# Complete Passkey Verification

🔐 Login Required (User Permission)

POST https://docs.newapi.pro

Endpoint

```
/`api`/`user`/`passkey`/`verify`/`finish`
```

Send

Response body reference: https://docs.newapi.pro/en/docs/api/management/user-management/user-passkey-verify-finish-post#response-body

### 200

cURL

```bash
curl -X POST "https://docs.newapi.pro/api/user/passkey/verify/finish"
```

200 — Empty

Last updated on 3/1/2026
