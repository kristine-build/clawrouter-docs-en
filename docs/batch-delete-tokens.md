# Batch Delete Tokens

Management APIToken management

## Batch Delete Tokens

🔐 Login Required (User Permission)

https://docs.newapi.pro

POST

`/`api`/`token`/`batch\`

Send

Body

### Request Body

application/json

ids?array

(See full schema: https://docs.newapi.pro/en/docs/api/management/token-management/token-batch-post#request-body)

### Response Body

(See details: https://docs.newapi.pro/en/docs/api/management/token-management/token-batch-post#response-body)

#### 200

cURL

```bash
curl -X POST "https://docs.newapi.pro/api/token/batch" \
  -H "Content-Type: application/json" \
  -d '{}'
```

200

Empty

Last updated on 3/1/2026
