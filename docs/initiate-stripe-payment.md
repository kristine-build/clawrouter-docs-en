# Initiate Stripe Payment

Documentation renewed! For old docs, visit https://doc.newapi.pro/

Management API — Payment

## Initiate Stripe Payment

{% hint style="info" %}
🔐 Login required (User permission)
{% endhint %}

Base URL: https://docs.newapi.pro

Method: POST

Endpoint:

```
/`api`/`user`/`stripe`/`pay
```

Request example (cURL):

{% code title="cURL" %}
```bash
curl -X POST "https://docs.newapi.pro/api/user/stripe/pay"
```
{% endcode %}

Response Body https://docs.newapi.pro/en/docs/api/management/payment/user-stripe-pay-post#response-body

#### 200

Empty

Last updated on 3/1/2026
