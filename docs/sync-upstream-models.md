# Sync Upstream Models

Documentation renewed! For old docs, visit https://doc.newapi.pro/

Management API — Model management

Sync Upstream Models

{% hint style="warning" %}
Requires Admin Permissions
{% endhint %}

Endpoint POST

\`\`/`api`/`models`/`sync_upstream`

Request POST https://docs.newapi.pro/api/models/sync\_upstream

Example

```bash
curl -X POST "https://docs.newapi.pro/api/models/sync_upstream"
```

Response

See the Response Body: https://docs.newapi.pro/en/docs/api/management/model-management/models-sync\_upstream-post#response-body

200 Empty

Last updated on 3/1/2026
